{
  "name": "linggen-vscode",
  "displayName": "Linggen",
  "description": "Linggen integration for VS Code - visualize architecture and enhance LLM workflows",
  "version": "0.1.0",
  "publisher": "linggen",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "linggen.install",
        "title": "🌀 Linggen: Install Linggen"
      },
      {
        "command": "linggen.indexCurrentProject",
        "title": "🌀 Linggen: Index Current Project"
      },
      {
        "command": "linggen.openGraphView",
        "title": "🌀 Linggen: Open Graph"
      },
      {
        "command": "linggen.configureCursorMsp",
        "title": "🌀 Linggen: Configure Cursor msp.json"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "linggen.openGraphView",
          "group": "navigation@11"
        }
      ],
      "editor/context": [
        {
          "command": "linggen.openGraphView",
          "group": "navigation@11"
        }
      ]
    },
    "configuration": {
      "title": "Linggen",
      "properties": {
        "linggen.backend.httpUrl": {
          "type": "string",
          "default": "http://localhost:8787",
          "description": "Base URL of the Linggen HTTP server (used for UI and MCP, e.g. {httpUrl}/mcp/sse)"
        },
        "linggen.installUrl": {
          "type": "string",
          "default": "https://linggen.dev",
          "description": "URL to open for Linggen installation instructions"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.x",
    "@types/mocha": "^10.0.6",
    "@types/glob": "^8.1.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.0",
    "@vscode/test-electron": "^2.3.8",
    "mocha": "^10.2.0",
    "glob": "^10.3.10"
  }
}